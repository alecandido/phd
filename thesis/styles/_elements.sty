% !TeX root = ../main.tex

% pseudo-text
% \RequirePackage[language=english]{lipsum}

% quotes
\RequirePackage[font=small]{quoting}

% complete page references
\RequirePackage[english]{varioref}

% smart references
\RequirePackage[sort]{cleveref}
\RequirePackage{nameref}

% better array structure
\RequirePackage{array}

% fixed width tables
\RequirePackage{booktabs}
\RequirePackage{tabularx}
\RequirePackage{multirow}
\RequirePackage{colortbl}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}

% lists
\RequirePackage{enumerate}
\RequirePackage{enumitem}
\setlist[description]{font=\quad\normalfont\itshape\space}

% images
\RequirePackage{graphicx}
\RequirePackage{subfig}

% symbols
\RequirePackage{pifont}
\let\checkmark\relax
\RequirePackage{dingbat}

% sub-figures and sub-tables
\RequirePackage{subfig}

% dates and times
\RequirePackage{datetime2}
\RequirePackage{datetime2-calc}

% Particles names
\RequirePackage{hepnames}

% International units
% \RequirePackage[cdot, thickqspace, squaren]{SIunits}
\RequirePackage{siunitx}
\sisetup{
	retain-unity-mantissa = false
}
% \AtBeginDocument{\RenewCommandCopy\qty\SI}

% ??? from EKO
\RequirePackage{wasysym}

% bibliography;
% make citing style author-year;
% the style "numeric-comp" makes numeric references;
\RequirePackage[style=philosophy-modern,url=false,
	hyperref,square,backend=biber]{biblatex}

% glossaries (after hyperref)
\RequirePackage[acronym,automake,nomain,section=section]{glossaries}
\renewcommand*{\glstextformat}[1]{\textcolor{black!90}{#1}}
\makeglossaries
