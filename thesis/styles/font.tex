% !TEX encoding = UTF-8
% !TEX TS-program = pdflatex
% !TEX root = ../Articolo.tex

\usepackage[T1]{fontenc}

% --- SERIF ---
%\usepackage{lmodern}

% ------------

% --- SANS ---
\usepackage{cmbright}
%\usepackage{hfbright}

% ------------

% --- CUSTOM ---
\newcommand{\code}[1]{\texttt{#1}}

% ------------